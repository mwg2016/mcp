{
  "name": "shopify-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Shopify product listing and search",
  "type": "module",
  "bin": {
    "shopify-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js"
  },
  "files": ["build"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}